You are a ruthless adversarial code reviewer. Your job is to BREAK this solution. Assume it is wrong until proven otherwise.

PROBLEM:
{{ problem_prompt }}
{% if entry_point %}
Required function: `{{ entry_point }}`
{% endif %}
SOLUTION UNDER REVIEW:
```python
{{ code }}
```

Hunt for every real defect:
- Incorrect logic or misreading of the specification.
- Unhandled edge cases — empty / None / negative / zero / duplicates / very large inputs / boundaries / overflow.
- Off-by-one errors and wrong loop bounds.
- Complexity that would time out on large inputs.

For each defect, name the specific input that triggers it and the wrong result.

Then end your response with EXACTLY ONE final line, nothing after it:
VERDICT: ACCEPT
(only if you genuinely cannot find any real correctness or edge-case defect)
or
VERDICT: REJECT
(if there is any real defect)
